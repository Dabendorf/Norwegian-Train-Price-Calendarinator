# This is the configuration
# The format is simple
# Every line is one configuration setting
# Lines like this one, starting with # are ignored
# Every other line consists of two information, separated by a tab (tab != 4spaces)
# The left is the name of the setting, the right is the content
# If there are more than one argument, they are comma separated
debug	True
transportTypes	tram,water,car_ferry,metro,flytog,flybuss
emailActivated	True
emailTo	whatever@gmail.com